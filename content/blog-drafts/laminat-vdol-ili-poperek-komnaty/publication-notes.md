# Выпуск 10 сентября 2026

Статья: https://getmasterok.ru/blog/laminat-vdol-ili-poperek-komnaty/

Ghost post ID: `6aa22b61c4aacd28544c73a2`. Перед созданием Admin API по slug вернул `found:false`; создание также проверяет существование slug. Создан draft с двумя WebP, просмотрен CMS preview, затем отдельный publish. Публикация: `2026-09-10T04:01:36.000Z`, без рассылки.

## Подтверждено

- Основной URL отвечает 200, canonical совпадает с адресом выше; robots `index, follow, max-image-preview:large`.
- Read-only verifier подтвердил BlogPosting/dates, ссылку из блога, редакцию HTML/RSS, sitemap и обложку. Revision: `f0da385c234c441c19cd624e21926948cb964a175f22395cc13f24397e4bb0de`.
- RSS pubDate: `2026-09-10T04:01:36+00:00`; sitemap lastmod: `2026-09-10T16:01:36.000+12:00` — один момент времени.
- Обложка `/content/images/2026/09/feature-6.webp`: 1536×1024. Схема `/content/images/2026/09/directions.webp`: 1440×1050. Обе полностью загрузились в браузере.
- Desktop: заголовок, обложка и описание просмотрены. Mobile 390×844, тёмная тема: заголовок, обложка, текст и схема просмотрены; scrollWidth 384 ≤ innerWidth 390. Переход из оглавления к первому разделу работает. Мелкие подписи схемы повторены в окружающем тексте; схема не монтажная карта.
- Полный `npm.cmd test`: 3349/3349, 208 файлов. `npm.cmd run lint`: без ошибок/предупреждений ESLint. Код приложения и формулы не менялись; build для текстового CMS-пакета не запускался.
- Ghost webhook создал пустой rebuild-коммит `8778251`; он принят через fast-forward без изменения рабочих файлов.

## Границы

Сначала основной URL отдавал 404, затем RSS не содержал новую статью. После обновления кэшей verifier прошёл: ложного «всё готово» по одному Ghost status не делали. Очередь обнаружила публикацию (snapshot 28), затем перешла с `done:6,pending:1` на `done:7`, без pending/blocked, healthy=true. Это подтверждает завершение публикационного задания, включая приём IndexNow по контракту worker, не индексацию. Индексация Google/Яндекса, Discover, позиции и трафик этой новой статьи ещё не подтверждены.

Чужие изменения `.codex/config.toml`, test-results и parity fixtures не включать в этот пакет. Flutter не затронут.

## Содержательная переработка, 10 сентября 2026

Публичный URL, canonical и исходная дата выпуска сохранены. Ghost принял новую редакцию в `2026-09-10T05:08:37.000Z`: пять WebP-изображений загружены заново в CMS, а безопасный загрузчик предварительно сверил `updated_at` и SHA-256 прежнего HTML и создал локальную резервную копию ревизии на VPS. Локальный предпросмотр финальной редакции: 1 217 слов и семь разделов. Техническая доставка проверяется отдельно после обновления кэшей; она не подтверждает индексацию, Discover или рост трафика.

### Визуальная переработка

В `2026-09-10T05:36:58.000Z` три плоских внутренние схемы заменены на 3D-иллюстрации. URL, canonical и дата выпуска не менялись; загрузчик сверил текущую ревизию и заново загрузил пять используемых изображений. До внешней проверки не считать это доказательством индексации или рекомендаций.
