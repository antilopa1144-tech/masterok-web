/**
 * Справочный контент страницы «Таймер схватывания».
 *
 * Таблица сроков собирается из CURING_PRESETS — тех же пресетов, которые
 * отсчитывает инструмент. Значения не дублируются руками: если пресет изменят,
 * страница изменится вместе с ним. Отдельно от пресетов живёт только FAQ.
 */

import { CURING_PRESETS } from "./presets";

export interface CuringFaqItem {
  question: string;
  answer: string;
}

export interface CuringCategoryGroup {
  category: string;
  icon: string;
  rows: Array<{ name: string; timing: string; tip: string }>;
}

/** Пресеты, которые имеет смысл показывать в справочной таблице. */
const TABLE_PRESET_IDS = CURING_PRESETS
  .filter((preset) => preset.id !== "custom")
  .map((preset) => preset.id);

/**
 * Пресеты, сгруппированные по категории работ, в порядке объявления.
 * «Свой таймер» пропускается: у него нет сроков, он не справочная информация.
 */
export function getCuringGroups(): CuringCategoryGroup[] {
  const groups: CuringCategoryGroup[] = [];
  for (const preset of CURING_PRESETS) {
    if (!TABLE_PRESET_IDS.includes(preset.id)) continue;
    let group = groups.find((item) => item.category === preset.category);
    if (!group) {
      group = { category: preset.category, icon: preset.icon, rows: [] };
      groups.push(group);
    }
    group.rows.push({ name: preset.name, timing: preset.description, tip: preset.tip });
  }
  return groups;
}

/**
 * Ответы опираются на сроки и подсказки пресетов, а не на собственные нормы:
 * числа здесь те же, что отсчитывает таймер.
 */
export const CURING_FAQ: CuringFaqItem[] = [
  {
    question: "Через сколько можно затирать швы после укладки плитки?",
    answer:
      "Плиточный клей схватывается примерно за 20 минут, но затирку делают не по схватыванию, а после набора прочности: ориентир таймера — через сутки. Замывать затирку нужно раньше, через 15–30 минут после нанесения, а полную прочность она набирает за 72 часа. Точные сроки смотрите в паспорте клея: у быстрых составов они короче.",
  },
  {
    question: "Когда можно класть покрытие на свежую стяжку?",
    answer:
      "Ходить по стяжке ЦПС можно примерно через сутки, а укладывать финишное покрытие — после набора прочности. Таймер даёт ориентир 28 дней для толщины 40–50 мм и приводит бытовую прикидку «1 мм слоя — 1 день сушки», которая для толстой стяжки даёт заметно более длинный срок. Ориентируйтесь на паспорт смеси: у разных составов и толщин срок отличается, а спешка приводит к трещинам и отслоению покрытия.",
  },
  {
    question: "Сроки в таблице — это норматив?",
    answer:
      "Нет. Это ориентиры таймера для температуры около 20 °C и нормальной влажности: они помогают не пропустить момент проверки поверхности. Приоритет всегда у инструкции производителя — у разных смесей сроки отличаются в разы, и паспорт конкретного мешка важнее любой общей таблицы.",
  },
  {
    question: "Можно ли ускорить высыхание обогревателем или пушкой?",
    answer:
      "Нет, и это одна из типовых ошибок. Гипсовую штукатурку тепловыми пушками сушить нельзя — пойдут трещины. У наливного пола первые сутки опасны сквозняки, у обоев — открытые окна: клей должен схватиться в стабильных условиях. Естественная сушка при постоянной температуре надёжнее любого ускорения.",
  },
  {
    question: "Что делать, если отсчёт закончился, а поверхность ещё сырая?",
    answer:
      "Проверьте поверхность так, как указано в подсказке к материалу: грунтовка не должна быть липкой, бетоноконтакт — стать сухим и шершавым на ощупь. Если сомневаетесь, запустите отсчёт заново или ориентируйтесь на паспорт смеси. Спешка на этом шаге приводит к переделке всего следующего слоя.",
  },
  {
    question: "Сколько сохнет наливной пол?",
    answer:
      "Ходить по наливному полу можно через 4–6 часов, а укладывать покрытие — через 7–14 дней: таймер даёт именно этот ориентир. Первые сутки важнее всего: не допускайте сквозняков и перепадов температуры, иначе поверхность схватится неравномерно и пойдёт волной.",
  },
  {
    question: "Зачем вообще таймер, если сроки написаны на упаковке?",
    answer:
      "На упаковке срок указан для одного слоя и идеальных условий, а в работе слоёв несколько и они идут подряд: грунтовка, шпаклёвка, краска. Таймер считает интервал от вашего старта и напоминает, когда проверить поверхность, чтобы не начинать следующий слой по сырому основанию.",
  },
];
