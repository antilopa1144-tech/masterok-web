# D5: навигация блога — 2026-09-09

## Локально реализовано

`BlogPostGrid` использует компактный подписанный select на телефоне и кнопки с aria-pressed на широком экране. Выбор сохраняется при смене ширины. Счётчик показывает размер выдачи, пустая выдача имеет объяснение и сброс фильтра. Заголовки увеличены до 16 px, сетка ограничена тремя колонками, нижняя строка карточки допускает перенос.

`blog-categories.ts` объединяет только известные названия «Инженерия» и «Инженерные системы», нормализует регистр и пробелы. Исходные категории Ghost, slug, canonical, Article JSON-LD и содержание статей не меняются. Различные темы плитки остаются отдельными.

## Проверки

- Lint passed; Vitest: 204 файла, 3322 теста passed, включая три новых теста категорий.
- Реальный компонент на временном локальном маршруте с тремя тестовыми статьями: фильтр инженерных систем даёт две статьи; после resize 360 → 1440 активная кнопка сохраняется; «Все» возвращает три статьи.
- На 360/390/768/1440 px в светлой и тёмной теме scrollWidth равен ширине viewport. Просмотрены окончательные скриншоты 360 light и 1440 dark (с отключёнными переходными анимациями).
- Временный маршрут удалён, тестовые статьи не публиковались. Полная локальная страница блога не проверена из-за отсутствующего подключения Ghost; защита от пустого ответа не ослаблялась.
- Production: `/blog/kak-rasschitat-paneli-na-otkosy/` при ширине 360 px не расширяет страницу. Таблица шириной 620 px находится в контейнере 328 px с overflow-x:auto. Переход «Калькулятор материалов для откосов» успешно открыл существующий калькулятор.
- Ссылка оглавления меняет hash и имеет существующую цель; окончательное положение после плавной прокрутки не проверено (мгновенное измерение не засчитывается).

## Ограничения и выпуск

Пользователь разрешил выпуск и удаление ненужного артефакта. Файл `output/publication-a-smoke-20260906/repo/.env.local.example` побайтово совпадает по SHA-256 с отслеживаемым корневым шаблоном: это копия из изолированного smoke-теста, а не рабочее окружение. Удалена только эта untracked-копия; исходный шаблон и рабочий `.env.local` сохранены. Содержимое и хеши не выводились. Восстановить копию можно из корневого шаблона. Блокировка выпуска снята; production-приёмка нового интерфейса остаётся отдельным этапом после push.

Остаётся production-проверка новых фильтров на полном наборе статей, окончательная прокрутка оглавления, проверка подписей и клавиатуры в рамках D5/D6. Физические устройства и изменение конверсии не проверялись.
