# D4: реальные превью инструментов — 2026-09-09

## Содержание пакета

На главную ниже основного поиска и преимуществ добавлен серверный компонент `ToolPreviews`: две карточки с настоящими снимками инструментов, понятной подписью примера и ссылкой на настройку собственной раскладки. Движки калькуляторов на главную не импортируются. Существующий каталог ссылок сохранён, его заголовок допускает перенос на узком экране.

Превью сняты с production в Chromium при viewport 1440×1000:

- `/instrumenty/raskladka-plitki/`: первый видимый img «Объёмная 2.5D-модель: плитка на стене, прямая»; стена 2500×2600 мм, плитка 600×300 мм, исходный PNG 580×351, 163299 байт.
- `/instrumenty/raskladka-laminata/`: img «Объёмная модель комнаты: палуба 1/3, натуральный дуб»; комната 3000×4000 мм, доска 1285×192 мм, исходный PNG 574×327, 29616 байт.

Файлы находятся в `public/images/tool-previews/`. Это снимки реального результата, не сгенерированные изображения и не обещание результата для любого помещения. Для обновления повторить настройки и снять только соответствующий img. Исходники вместе 192915 байт; Next Image использует адаптивные размеры и отложенную загрузку. Контейнер aspect-ratio резервирует место до загрузки. Это технические меры, не замер Core Web Vitals или гарантия отсутствия любого изменения скорости.

## Проверки

- Lint passed; полный Vitest — 203 файла, 3319 passed.
- Production D3: на каталоге присутствуют все четыре группы с количеством 8/2/5/6.
- Локальная главная возвращает 500 из-за отсутствующего подключения Ghost. Защита от пустого блога не изменялась.
- Компонент проверен на временной локальной странице с теми же layout и контейнером. Страница удалена до коммита. Обе картинки декодируются, loading=lazy, переполнения нет на 360/390/768/1440 px. Светлый/тёмный варианты и скриншоты 360/1440 просмотрены.
- Локальные переходы карточек не засчитаны: клиентский RSC-запрос вернул 500, хотя прямой запрос инструмента возвращает 200. Нужна отдельная проверка полной главной и переходов после production-выкладки.
- Скриншоты проверки находятся в `output/playwright/d4-component-*.png` и `d4-dark-*.png`, в Git не включаются.

Дополнение: commit `9ef4ab5`, CI `34338636009` passed. На production появился блок «Посмотрите раскладку до покупки», обе картинки декодируются. Переход из превью плитки успешно открыл `/instrumenty/raskladka-plitki/`; локальная RSC-ошибка не повторилась. Переход ламината отдельно ещё не проверен.

Дополнительная приёмка D6: переход из реального превью ламината на главной открыл `/instrumenty/raskladka-laminata/`. Обе ссылки превью теперь проверены на production.

Не менялись формулы, URL инструментов, canonical и источники блога.
