# GSC: аудит исключённых URL — 15 августа 2026

## Снимок отчёта

Отчёт Page indexing в Google Search Console на момент проверки:

- 127 страниц в индексе;
- 109 URL не индексируются;
- дата обновления отчёта в интерфейсе GSC: `07/08/2026`;
- успешный live-тест `/kalkulyatory/poly/styazhka/` подтвердил доступность URL для Google и разрешённую индексацию;
- для `/kalkulyatory/poly/styazhka/` отправлен запрос на приоритетный повторный обход.

## Классификация причин

| Причина GSC | URL | Диагноз и действие |
|---|---:|---|
| Excluded by `noindex` | 67 | В основном тонкие Ghost-теги, служебный инструмент и старые aliases. Для них `noindex` или редирект являются ожидаемым поведением. |
| Duplicate without user-selected canonical | 9 | Параметризованные состояния инструментов канонизируются на основные страницы. Буквальный `/?q={search_term_string}` создавался устаревшим `SearchAction`; разметка удалена. |
| Page with redirect | 3 | Ожидаемые постоянные редиректы старых URL. |
| Not found (404) | 2 | Мусорные адреса `/$` и `/&`; сохраняем честный 404, не перенаправляем на главную. |
| Crawled — currently not indexed | 26 | Часть — технические XML/TXT/manifest/favicon URL. Для HTML-страниц live-тест подтвердил отсутствие технического запрета; нужен повторный обход и наблюдение за качеством/спросом. |
| Blocked by robots.txt | 1 | Старый URL шрифта `/_next/static/media/*.woff2`; текущий `robots.txt` разрешает `/_next/`, проверка исправления уже запущена в GSC. |
| Blocked due to access forbidden (403) | 1 | Старый URL категории; проверка исправления в GSC уже пройдена. |

## Исправления в коде

1. Добавлен постоянный редирект:
   `/kalkulyatory/fasad/zaborny-kalkulyator/` → `/kalkulyatory/fasad/zabor/`.
2. Удалён `SearchAction` из WebSite JSON-LD: Google прекратил поддержку sitelinks search box, а шаблон создавал буквальный дубль URL в отчёте.
3. Пользовательский поиск главной с реальными `?q=` оставлен без изменений.

## Проверка sitemap

Публичные ответы проверены напрямую:

- `/sitemap.xml` — `200`, валидный `sitemapindex`;
- `/sitemap/0.xml` … `/sitemap/4.xml` — все `200`;
- дочерние карты содержат суммарно 138 URL;
- `/robots.txt` — `200`, запрещён только `/api/`.

Показатель `0 discovered pages` у отправленного sitemap не подтверждается ответами сервера. Без дополнительного доказательства архитектуру sitemap не меняем; после деплоя повторно отправляем существующий `/sitemap.xml` в GSC и наблюдаем обновление отчёта.
