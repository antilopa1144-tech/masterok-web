# SEO-аудит волн 2 и 3: мета, контент, перелинковка

11 сентября 2026, 18:14–19:20 (локальное время машины). Источник данных — репозиторий и локальная production-сборка (`npm run build` + `next start`). Панели GSC, Яндекс Вебмастера и Метрики в этом проходе недоступны, поэтому все утверждения ниже — из кода, собранного HTML и живых HTTP-ответов, а не из поисковых метрик.

## Границы доказательств

- 148 URL из sitemap. Из них 114 не зависят от Ghost и проверены локально полностью; 33 страницы блога и тегов требуют Ghost Content API, которого нет в локальном окружении. Для них доказательство — зелёная сборка CI с секретами Ghost и ранее снятый ответ прода.
- Блог и теги локально отдают ошибку Ghost, поэтому в таблице мета они исключены, а не «прошли проверку».
- Холодная локальная сборка без `GHOST_API_URL` и `GHOST_CONTENT_API_KEY` останавливается на `/llms.txt`: маршрут статический и тянет список статей. Ранее сборка проходила за счёт тёплого `.next/cache`; после его очистки это воспроизводится. Для локальной проверки страниц без Ghost используется `next dev`.
- Production на момент отчёта отдаёт сборку от 15:45 (`eb97cf2`). Коммиты 18:14–19:30 на прод не приехали (см. «Доставка»).

## Волна 2 — мета-теги

Проверка собранного HTML по 114 не-блоговым URL:

| Метрика | Результат |
|---|---|
| Title длиннее 60 символов | 0 |
| Title пустой | 0 |
| Максимальная длина title | 60 |
| Description вне 120–165 | 0 (минимум 120, максимум 165) |
| Страниц не с ровно одним тегом `robots` | 0 |
| Дубль «— Мастерок — Мастерок» в title | 0 |

Что было исправлено до этой проверки: шаблон `%s — Мастерок` в корневом layout дописывал бренд всем страницам безусловно; суффикс перенесён в `withSiteSuffix()` и добавляется, только если заголовок остаётся в пределах 60 символов. Дубль бренда убран на `/proekty/`, `/o-proekte/`, `/ai/`.

### Блог и теги

34 блоговых URL локально не проверяются: контент приходит из Ghost. Проверено на живом проде, где ещё стоит старая сборка:

| Метрика (прод, старая сборка) | Значение |
|---|---|
| Title длиннее 60 | 18 |
| Из них с хвостовым « — Мастерок» | 18 |
| База заголовка длиннее 60 | 0 |
| Description вне 120–165 | 4 (все — страницы тегов, 102–115) |

Все 18 длинных заголовков перебирают лимит именно из-за суффикса бренда, а их база укладывается в 60 — значит после деплоя `withSiteSuffix()` суффикс снимет и инвариант выполнится. Ни одного случая, где сама CMS-заготовка длиннее 60, нет.

Проверено симуляцией, а не рассуждением: базовые заголовки всех 34 блоговых URL прода прогнаны через настоящий `withSiteSuffix` из сборки. Результат — 18 заголовков длиннее 60 в текущей сборке, все 18 чинятся снятием суффикса, останется длиннее 60: 0.

Описания четырёх страниц тегов в старой сборке короткие. Логика их сборки жила прямо в `generateMetadata` и не проверялась. Она вынесена в `buildTagTitle` и `buildTagDescription` (`src/lib/blog-tag-meta.ts`) и покрыта тестом: 80 длин имени тега × 7 значений числа статей держат title ≤ 60 и description в 120–165. Прогон новых функций на живых данных прода — все четыре тега в диапазоне (title 34–37, description 157–159).

Без этого длинное имя тега, созданное в Ghost, выбивало бы страницу из диапазона: `dynamicParams = true` пускает новые теги без пересборки сайта.

## Волна 3 — контент, AEO, перелинковка

### Сироты и входящие ссылки

| Показатель | Было | Стало |
|---|---|---|
| Калькуляторов без своей записи в карте связей | 14 | 0 |
| Калькуляторов без входящей контекстной ссылки | 6 | 0 |
| Страниц со ссылкой на `/ai/` среди ответивших | — | 48 из 48 |

Ссылка на `/ai/` стоит в подвале, то есть присутствует на каждой странице сайта. Проверка: 48 успешно отвечающих URL локальной сборки, ссылка найдена на всех.

### llms.txt

Покрытие расширено с 15 популярных калькуляторов до всех 65, добавлен блок «Частые вопросы» и ссылки на `/ai/` и `/metodologiya/`. Число «65+» заменено точным «65». Держится тестом `src/app/llms.txt/route.test.ts`: сверяет каждый slug каталога, отсутствие `N+` и наличие блоков.

### AEO: вопросные заголовки

FAQ-вопросы на страницах калькуляторов были `h3` под заголовком раздела, то есть в оглавлении страницы вопросов не было вообще. Теперь вопрос — `h2`, сразу за ним ответ в DOM.

Проверка локальной сборки: 65 из 65 страниц калькуляторов содержат вопросные `h2`, всего 399 вопросов. Структура держится тестом `src/components/seo/SeoContentBlock.test.ts`.

### Тонкие страницы

Замер одинаковым способом: вырезаны `script`, `style`, `nav`, `footer`, `header`. Столбец «прод» — текущая старая сборка, «локально» — новая.

| Страница | Прод | Локально |
|---|---:|---:|
| `/proekty/` | 34 | 192 |
| `/prilozhenie/` | 181 | 518 |
| `/instrumenty/kalendar-remonta/` | 181 | 1016 |
| `/instrumenty/tajmer-skhvatyvaniya/` | 246 | 967 |
| `/instrumenty/konverter/` | 221 | 509 |
| `/kalkulyatory/krovlya/` | 229 | 453 |
| `/kalkulyatory/fundament/` | 416 | 545 |
| `/kalkulyatory/steny/` | 415 | 534 |
| `/kalkulyatory/poly/` | 446 | 566 |
| `/kalkulyatory/fasad/` | 494 | 599 |
| `/kalkulyatory/inzhenernye/` | 402 | 556 |
| `/kalkulyatory/otdelka/` | 617 | 773 |
| `/kalkulyatory/potolki/` | 251 | 466 |

Таблица этапов на календаре и таблица сроков на таймере собираются из `RENOVATION_SCENARIOS` и `CURING_PRESETS` — тех же данных, что используют инструменты, поэтому страница не может разойтись с расчётом.

`/blog/tag/raskladka/` остаётся `noindex` (1 статья, порог индексации — 3). Страницам тегов добавлено пояснение к подборке и блок «Калькуляторы по теме»; логика ссылок вынесена в `getTagCalculatorLinks` и покрыта тестом, включая отбрасывание slug, которого нет в каталоге.

## Найденные дефекты, не связанные с мета

**Ответы FAQ на страницах категорий отсутствовали в HTML.** Аккордеон был клиентским (`useState`), ответ попадал в DOM только после клика, при этом `FAQPage`-разметка ответы описывала. Живая проверка `/kalkulyatory/krovlya/` до правки: вопрос в DOM есть, ответа нет. Исправлено переводом на серверные `<details>`.

**Единица измерения в блоке «Потребность и покупка».** `buy_plan.unit` описывает упаковку, а `purchase_quantity` выражен в единицах материала. Клей показывал «125 мешков» вместо 125 кг, арматура — «1 790,1 прутков» вместо погонных метров, шпаклёвка — «60 kg». Исправлено выбором единицы материала, у которого совпадают число и размер упаковки. Расчёты, запас и округление не менялись.

**«Рекомендуем 0 отрезков»** в вентиляции при незаданной длине трассы. Блок покупки теперь не рендерится при нуле.

## Доставка

Все правки в `main`, CI зелёный на каждом коммите: `92a696f`, `a15d558`, `10f9d7b`, `c08ceab`, `d185b00`, `d4a7e2a`, `fdba1c9`, `d0529f9`, `766de22`, `b28f8b3`, `170332f`. Тесты 3431 → 3472.

Проверка живых URL вынесена в одну команду: `npm run verify:live`. Она проверяет семь правил — robots для AI-краулеров, покрытие `llms.txt`, дубль бренда в title, ответы FAQ в HTML категорий, вопросные h2 на калькуляторах, новые блоки усиленных страниц и лимиты title/description по всем URL sitemap. Из HTML перед поиском маркера вырезаются `script` и `style`, иначе JSON-LD выдаёт себя за видимый текст.

Прогон `npm run verify:live` 11 сентября 2026: 7 из 7 проверок падают, 64 расхождения по 148 URL. Это ожидаемо и означает ровно одно — прод отдаёт старую сборку.

Production отдаёт сборку от 15:45. Проверка живых маркеров: `/ai/` возвращает «Что такое Мастерок — справка о сайте — Мастерок» (дубль бренда), `llms.txt` — «65+» без блока «Частые вопросы», ответы FAQ на категориях не в DOM, `robots.txt` без новых AI-краулеров. Вебхук Timeweb принял все push-события (HTTP 201 по данным GitHub), то есть доставка события работает, а сборка не выпускается. Со стороны репозитория препятствий нет: `origin/main` совпадает с локальным, других веток деплоя и deploy-конфигов в репозитории нет.

Состояние вебхука на 11 сентября, 07:40 UTC: 40 доставок за сутки, 39 с ответом 201 и одна с 500 (07:18 UTC). Последняя успешная сборка — 03:45 UTC. Повторная доставка последнего события (`POST …/deliveries/{id}/attempts`, ответ 201) сборку не запустила: за девять минут наблюдения `robots.txt` не изменился. Значит проблема не в доставке события, а в обработке или очереди сборок на стороне приложения.

Кэш проверен отдельно: `/robots.txt` отдаётся с `x-nextjs-cache: HIT` и `max-age=86400`, `/llms.txt` — с `x-nextjs-cache: STALE`, а `/proekty/` идёт с `cache-control: no-store` и без заголовка кэша Next. Динамическая страница рендерится процессом на каждый запрос, и она отдаёт старый заголовок — значит работает старый процесс с прежней сборкой, а не отдаётся кэш. Если бы сборка вышла, страница с `no-store` показала бы новый заголовок сразу.

Отсюда две возможные причины, обе устраняются в панели:

1. Сборка не запускается — проверить авто-деплой ветки `main` и лог сборки (очередь, лимит минут, пауза после ошибки).
2. Сборка проходит, но процесс не перезапущен — тогда нужен Restart / Redeploy приложения.

После выпуска: `npm run verify:live` должен вернуть 0 и напечатать «Все проверки пройдены».

## Повторная оценка

Через 2–4 недели после деплоя: индексация страниц категорий (`FAQPage` без ответов в контенте — риск снятия rich-результатов), показы вопросных запросов на страницах калькуляторов, поведение страниц тегов. Панели для этого прохода недоступны; метрики нужно снимать из GSC, Вебмастера и Метрики.
