# D6: промежуточная приёмка — 2026-09-09

## Production-проверки

- D5 (`68f7adb`) виден на `/blog/`: 26 статей, объединённые инженерные системы — 2, плитка — 3. Desktop-кнопка → resize 360 px сохраняет тему; мобильный select переключает тему и возвращает все 26 статей. ScrollWidth 360 px. Скриншот `output/playwright/d5-production-360.png` просмотрен.
- На главной поиск «ламинат» → ArrowDown → Enter открывает `/kalkulyatory/poly/laminat/`.
- Расчёт по дефолту показывает потребность 24,28 м², покупку 26,37 м², остаток 2,09 м² и 11 упаковок по 2,397 м². Это проверка отображения, не новый аудит формулы.
- «В проект» → создать «Проверка D6 — тестовый проект» → остаться → reload → открыть проекты: тестовый проект присутствует с одним расчётом. Использована отдельная браузерная сессия, не личный профиль пользователя.
- Статья `/blog/kak-rasschitat-paneli-na-otkosy/` сохраняет правильный canonical и содержит JSON-LD. Полная семантическая проверка JSON-LD этим не заменяется.

## Найденный и исправленный дефект

После клика оглавления «Какие три размера снять с одного окна» конечная позиция заголовка — 0 px, низ закреплённой шапки — 65 px: заголовок перекрыт. Промежуточное положение во время плавной прокрутки не доказывает исправность.

В `prose-custom` добавлен scroll-margin-top:6rem только для h2/h3/h4 с id. Исходные HTML, URL, Ghost, формулы не менялись. На временном локальном маршруте с настоящим layout и CSS после клика положение около 96 px на viewport 360/1440, шапка 65 px. Повторное измерение после завершения анимации: 95,59 px, computed margin 96 px. Скриншот 360 просмотрен. Временный маршрут удалён до commit. Production-проверка исправленного отступа требуется после выкладки.

## Дополнительная production-приёмка

- CI исправления `431ca9c`: `34344122014`, conclusion success. На опубликованной статье после завершения прокрутки заголовок стоит на 96 px, шапка заканчивается на 65 px. Скриншот `output/playwright/d6-anchor-production.png` просмотрен: заголовок не перекрыт.
- `/proekty/` → «Открыть смету» тестового проекта: открылись 10 позиций, в том числе 11 упаковок ламината. «Реквизиты и экспорт» → CSV скачивает файл без ошибки, 1586 байт. Проверено наличие всех 10 строк материалов и количества 11 упак.; пустые цены сохранены пустыми. Нативный Excel и печать не проверялись. Тестовый CSV не включён в Git.
- Смета на 360 px: scrollWidth 360 px.
- Главная → реальная карточка-превью ламината → `/instrumenty/raskladka-laminata/`: переход прошёл, оставшаяся проверка D4 закрыта.
- В тестовом Chromium: cache disabled, latency 400 ms, download 200000 bytes/s, upload 100000 bytes/s. Загрузка ламината → «Рассчитать» → видимый результат заняла 6229 ms. Настройки восстановлены в finally. Это одиночный функциональный тест, не Core Web Vitals и не статистика реальных пользователей.
- Через меню переключены Тёмная/Тёплая/Океан/Светлая: результат сохранён, scrollWidth не превышает 360 px. Снимки тёмной и океанской тем просмотрены, покупка и пояснение читаются. Это не замер полного WCAG-контраста.

## Ещё не закрыто

### Приёмка краски и септика

- Краска, 360 px: −1/0/1001 м² помечаются aria-invalid, кнопка меняется на «Исправьте параметры», прежний результат скрыт. Возврат к 40 м² даёт потребность 8,8 л, покупку 9 л, остаток 0,2 л; 80 м² — 17,6/18/0,4 л. Пустой draft на blur восстанавливает последнее значение 40. Это текущее поведение поля, не ошибка формулы.
- Септик, production: дефолт показывает минимум 2,4 м³; выбранные 2 м³ дают предупреждение «не хватает 0,4 м³». Режим проектного притока 1,1 м³/сут и выбранной системы 3,5 м³ показывает минимум 3,3 м³. Нулевое ЭЧЖ помечается ошибкой и блокирует расчёт. Переполнения на 360 px не обнаружено.
- Обнаружена неточность представления: универсальный блок закупки интерпретировал резерв вместимости септика как «остаток» материала и показывал упаковку объёма. Для canonicalSpecId=sewage этот дублирующий блок теперь не выводится: отдельные карточки минимального/выбранного объёма, предупреждения и список заданных материалов сохраняются. Формулы, значения результата и canonical-схема не менялись.
- Regression: сначала тест воспроизвёл лишний блок; после правки прошли оба рендер-теста (септик скрывает блок, материалы сохраняют его), lint и все 3324 теста. Полная локальная страница вернула 500, поэтому её визуальная проверка не засчитана. После выкладки нужна production-проверка исправления.

- Печать списка закупки, переход к связанному инструменту непосредственно из результата калькулятора.
- Полная клавиатурная приёмка, проверка фокуса и измерения контраста ключевых элементов.
- Физические устройства; полная матрица всех комбинаций краски/септика не выполнялась.
- Сопоставимые периоды аналитики: свежие показатели здесь не получены, рост конверсии/SEO не заявляется.

D6 остаётся открытым. Этот пакет фиксирует production-приёмку D5, начало сквозного пути и устранение подтверждённого дефекта оглавления.
